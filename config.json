{
  "_comment": "Global rust-conductor backend configuration. Mounted at /etc/rust-conductor/config.json. Environment variables (when set) always take precedence over values here. Remove a key to fall back to env/default.",

  "host": "0.0.0.0",
  "port": 8090,
  "grpcPort": 50055,

  "slowQueryThresholdMs": 500,
  "defCacheMaxEntries": 1000,
  "sweeperMinIntervalSecs": 10,
  "sweeperMaxIntervalSecs": 60,

  "rateLimit": {
    "enabled": true,
    "maxRequests": 5000,
    "windowSecs": 60
  }
}
